2026 8 27 Thursday

3-2
3D로 차이점 표현

3-3
matplotlib - 라이브러리를 활용

__all__

```python

def rot_x(theta: float) -> np.array:
    c, s = np.cos(theta), np.sin(theta)
    return np.array([[1.0, 0.0, 0,0],
                      [0.0, c,  -s]
])
```
def rot_y
```
suppress=true
```
- 1.234e-10 --> 오류
- 0에 가깝게 하는 기수 표현법을 쓴다. 

---

```
rcParams["font.family"] = _f
```
- 한글 폰트를 고를 수 있게 해줌

---

```
def check(label1, condition)
```

def draw_frame()

scale 변경, 단위 변경을 원하면 numpy에서 찾아봐야 한다

3.2-2
pi plot은 제공할 예정. 어떤 성분을 쓸지 골라서 쓰면 됨

3.2-3

